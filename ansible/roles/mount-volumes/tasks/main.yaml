---
- name: Format volumes
  become: yes
  filesystem:
    fstype: xfs
    dev: "{{ volume }}"

- name: Mount volumes on nodes
  become: yes
  mount:
    path: "{{ mountpoint }}"
    src: "{{ volume }}"
    fstype: xfs
    state: mounted