- hosts: localhost
  vars_files:
    - host_vars/vars.yaml
  gather_facts: true

  roles:
    - role: openstack-volume
    - role: openstack-security-group
    - role: openstack-instance

- hosts: COMP90024
  vars_files:
    - host_vars/vars.yaml
  gather_facts: true
  become: yes

  roles:
  - role: common
  - role: install-docker
  - role: init-couchdb
  - role: set-couchdb-cluster